-r ../requirements.base.txt
# Use platform-specific TensorFlow packages to avoid crashes on Apple Silicon.
# - On macOS arm64: use tensorflow-macos
# - On all other platforms (including Linux/Docker): use standard tensorflow
tensorflow>=2.16,<3.0; sys_platform != "darwin" or platform_machine != "arm64"
tensorflow-macos>=2.12,<3.0; sys_platform == "darwin" and platform_machine == "arm64"
numpy>=1.26.4,<2.0
